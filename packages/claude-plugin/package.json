{
  "name": "@obsidian-vfs/claude-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/hook-handler.d.ts",
      "import": "./dist/hook-handler.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm exec tsc -b tsconfig.build.json && node esbuild.mjs",
    "clean": "pnpm exec tsc -b --clean tsconfig.build.json && rm -rf bundle"
  },
  "dependencies": {
    "@obsidian-vfs/core": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
