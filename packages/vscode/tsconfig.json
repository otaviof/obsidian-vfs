{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    // Source is ESM but esbuild outputs CJS for VSCode; disable to allow ESM
    // import syntax in a package without "type": "module".
    "verbatimModuleSyntax": false
  },
  "include": ["src"],
  "references": [{ "path": "../core" }]
}
